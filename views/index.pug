extends layout.pug

form(class="form-signin")
    h1(class="h3 mb-3 font-weight-normal") Please sign in
    label(for="email" class="sr-only") Email address
    input(type="email" id="email" class="form-control" placeholder="Email address" required autofocus)
    label(for="password" class="sr-only") Password
    input(type="password" id="password" class="form-control" placeholder="Password" required)
    a(class="btn btn-lg btn-primary btn-block" onClick="signIn()") Sign in
    a(href="/signup.html") Don't have an account? Sign up here.
    a(href="/forgot-password.html") Forgot password? Reset it here.